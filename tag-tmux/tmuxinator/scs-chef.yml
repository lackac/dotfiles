# ~/.tmuxinator/scs-chef.yml
# you can make as many windows as you wish...

name: "SCS Chef ﲮ"
root: ~/Code/Sensata/scs-chef-repo
startup_window: shell
windows:
  - :
      layout: main-horizontal
      panes:
        - vim
  - knife ﲵ:
  - shell: git pull
