# ~/.tmuxinator/scs-frontend.yml
# you can make as many windows as you wish...

name: "SCS Frontend "
root: ~/Code/Sensata/scs-frontend
startup_window: shell
windows:
  - :
      layout: main-horizontal
      panes:
        - vim
  - ﲵ:
  - shell: git pull
  - :
      index: 8
      layout: main-horizontal
      panes:
        - PORT=5050 npm start
