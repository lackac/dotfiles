# ~/.tmuxinator/scs-backend.yml
# you can make as many windows as you wish...

name: "SCS Backend "
root: ~/Code/Sensata/scs-backend
startup_window: shell
windows:
  - :
      layout: main-horizontal
      panes:
        - vim
  - ﲵ: iex -S mix
  - shell: git pull
  - :
      index: 8
      layout: main-horizontal
      panes:
        - mix phx.server
